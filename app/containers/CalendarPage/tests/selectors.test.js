// import { selectCalendarPageDomain } from '../selectors';

describe('selectCalendarPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
