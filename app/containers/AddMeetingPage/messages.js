/*
 * AddMeetingPage Messages
 *
 * This contains all the text for the AddMeetingPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddMeetingPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddMeetingPage container!',
  },
});
